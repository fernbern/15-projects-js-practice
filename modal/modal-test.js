// select modal-btn,modal-overlay,close-btn
// listen for click events on modal-btn and close-btn
// when user clicks modal-btn add .open-modal to modal-overlay
// when user clicks close-btn remove .open-modal from modal-overlay

const modalBtn = document.querySelector(".modal-btn");
const modal = document.querySelector(".modal-overlay");
const closeBtn = document.querySelector(".close-btn");

modalBtn.addEventListener("click", function () {
  modal.classList.add("open-modal");
modal.classList.add(o"open-modal");
});
closeBtn.addEventListener("click", function () {
  modal.classList.remove("open-modal");
});
const modalBtn = docuemnt.querySelector('.modal-btn');
const modal = document.querySelector('.modal-overlay');
const closeBtn = document.querySelector('.close-btn');
modalBtn.classList.addEventListener('click', function() {
  modal.classList.add('open-moda;');
})
closeBtn.addEventListener('click', function() {
  modal.classList.remove('open-modal');
})
const modalBtn = document.querySelector("modal");
const modal = document.querySelector("fer");
// make until is it musle memory
const modal = document.querySelector('.modal-btn');
const modalBtn = document.querySelector('.modal-btn');
const modal = document.querySelector('.modal-overlay');
const closeBtn = document.querySelector('slose-btn');
modalBtn.addeventListener('click' function() {
  modal.classList.add('open-modal');
})
